package global

import (
	"github.com/AlyxPink/meower/web/routes"
	"github.com/gofiber/fiber/v2"
)

templ Navigation(c *fiber.Ctx) {
	<nav>
		<ul class="flex flex-inline space-x-6">
			<li><a class="underline" href={ templ.SafeURL(c.App().GetRoute(routes.MeowNew.Name).Path) }>Create new Meow</a></li>
			<li><a class="underline" href={ templ.SafeURL(c.App().GetRoute(routes.MeowIndex.Name).Path) }>List Meows</a></li>
		</ul>
	</nav>
}
